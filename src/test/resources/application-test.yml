spring:
  application:
    name: session

  datasource:
    url: jdbc:postgresql://localhost:2348/session
    username: postgres
    password: 134679002
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: false

  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/changelog-master.xml
    drop-first: false
    contexts: test
